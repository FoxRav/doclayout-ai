# Requirements

| File | Purpose |
|------|---------|
| `ocr-extras.txt` | OpenCV, ONNX GPU, pypdfium2 — installed by `scripts/setup.ps1` |
| `ocr-vl.txt` | PaddleOCR-VL doc-parser — installed by `scripts/install_paddleocr_vl.ps1` |

Core package deps are in `pyproject.toml` (`pdf`, `dev` extras).
